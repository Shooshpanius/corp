#!version:1.0.0.1

##File header "#!version:1.0.0.1" can not be edited or deleted, and must be placed in the first line.##

<% IpPhone.where('mac = ?', @form_data[:device].mac).each_with_index do |device, index|   %>
    <%

       @index = index+1
       if @form_data[:device].corp_number != nil and @form_data[:device].corp_number != 0
        @active = 1
       else
        @active = 0
       end

       if @active = 1

         fio_arr = AddressBookCorp.find(CorpNumber.find_by_number(@form_data[:device].corp_number).address_book_corp_id).fio.split(' ')

            @fio_init = fio_arr[0] + " " + fio_arr[1][0] + "." + fio_arr[2][0] + "."
            @login = @form_data[:device].corp_number
            @password = '1000'
            @sip_server_host='192.168.2.40'
            @sip_server_port='5060'




       else
         @fio_init = ''

       end


    %>

    <%= render "serv/autoprovision/get_mac_yealink_cfg" %>
<% end %>

<%= render "serv/autoprovision/get_mac_yealink_cfg" %>